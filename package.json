{
  "name": "discord_deploy",
  "version": "0.1.9",
  "bin": {
    "discord_deploy": "./cli.js"
  },
  "description": "Deploy slash commands easily.",
  "main": "cli.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon",
    "build": "esbuild cli.js --minify --platform=node --format=esm  --packages=external --outdir=bin",
    "prepack": "npm run build",
    "prepublish:npm": "npm run build",
    "publish:npm": "npm publish --access public"
  },
  "keywords": [],
  "author": "felipee-monteiro",
  "license": "ISC",
  "devDependencies": {
    "discord.js": "^14.7.1",
    "nodemon": "^2.0.20",
    "prettier": "2.8.2"
  },
  "dependencies": {
    "@discordjs/rest": "^1.5.0",
    "ansi-styles": "^6.2.1",
    "commander": "^9.5.0",
    "discord_deploy": "^0.1.9",
    "discord-api-types": "^0.37.27",
    "dotenv": "^16.0.3",
    "esbuild": "^0.17.0",
    "fast-glob": "^3.2.12",
    "normalize-path": "^3.0.0"
  }
}
